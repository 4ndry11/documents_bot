# 📄 AI-Powered Document Collection System

**Інтелектуальна система збору та валідації документів клієнтів з AI-перевіркою та аналітикою**

![Python](https://img.shields.io/badge/Python-3.9+-blue?logo=python&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-13+-336791?logo=postgresql&logoColor=white)
![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4o%20Vision-412991?logo=openai&logoColor=white)
![Telegram](https://img.shields.io/badge/Telegram-Bot%20API-26A5E4?logo=telegram&logoColor=white)
![Google Drive](https://img.shields.io/badge/Google%20Drive-API-4285F4?logo=googledrive&logoColor=white)

---

## 🎯 Про проєкт

Розробив повноцінну систему автоматизованого збору документів клієнтів для юридичної компанії, яка спеціалізується на процедурах банкрутства. Система включає AI-валідацію документів через GPT-4 Vision, автоматичну організацію файлів на Google Drive, та комплексну аналітику процесу збору.

**Бізнес-проблема:** Ручний збір документів від клієнтів займав тижні, менеджери витрачали до 60% робочого часу на комунікацію та перевірку коректності завантажених файлів.

---

## 🏗️ Архітектура системи

```
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│     CLIENT       │     │    TELEGRAM      │     │   AI VALIDATION  │
│                  │────▶│      BOT         │────▶│                  │
│  Telegram App    │     │  Document Flow   │     │  GPT-4o Vision   │
│                  │     │  Progress Track  │     │  PDF Analysis    │
└──────────────────┘     └────────┬─────────┘     └────────┬─────────┘
                                  │                        │
                                  ▼                        ▼
                         ┌──────────────────┐     ┌──────────────────┐
                         │   POSTGRESQL     │     │  GOOGLE DRIVE    │
                         │                  │     │                  │
                         │  Client Data     │     │  Organized       │
                         │  Documents Log   │     │  File Storage    │
                         │  AI Validations  │     │  Client Folders  │
                         └────────┬─────────┘     └──────────────────┘
                                  │
                                  ▼
                         ┌──────────────────┐
                         │    GRAFANA       │
                         │                  │
                         │  KPI Dashboard   │
                         │  Problem Docs    │
                         │  Team Analytics  │
                         └──────────────────┘
```

---

## ✨ Ключові можливості

### 🤖 AI-валідація документів (GPT-4o Vision)

Інтегрував **OpenAI GPT-4o Vision API** для автоматичної перевірки документів:

- **Двоетапна перевірка**: якість зображення → тип документа
- **9 типів документів** з унікальними промптами валідації
- **Підтримка PDF** через OpenAI Responses API
- **"Лояльна" валідація** — система приймає документи різних форматів (фото телефону, скріншоти, скани)

```python
# Приклад результату валідації
ValidationResult(
    status='rejected',
    error_code='foreign_passport',  # Закордонний замість внутрішнього
    reason='Потрібен внутрішній паспорт'
)
```

**Типи помилок, які розпізнає AI:**
| Код | Опис | Дія |
|-----|------|-----|
| `poor_quality` | Розмите зображення | Перезавантажити |
| `wrong_document` | Не той тип документа | Пояснення + інструкція |
| `foreign_passport` | Закордонний паспорт | Потрібен внутрішній |
| `drivers_license` | Водійське замість паспорта | Чітке пояснення |
| `uncertain` | Потребує ручної перевірки | Передається менеджеру |

### 📱 Telegram Bot з прогрес-трекінгом

- **Візуальний чек-лист** з прогрес-баром для кожного клієнта
- **9 обов'язкових + 4 додаткових** типи документів
- **Відео-інструкції** для кожного типу документа
- **Анкета декларації** з 17 питаннями (збереження прогресу)
- **Дедуплікація** та захист від повторних завантажень
- **Автоматичні нагадування** неактивним клієнтам (3, 6, 9+ днів)

### 📁 Автоматична організація на Google Drive

- **Структурована папка клієнта**: `Прізвище Ім'я | +380XXXXXXXXX`
- **Підпапки по категоріях**: Особисті документи, Кредитні договори, Декларація, тощо
- **Автоматичне перейменування файлів**: `Паспорт_Іванов_Петро.pdf`
- **OAuth 2.0 + Service Account** підтримка

### 📊 Аналітика в Grafana

Побудував дашборд з ключовими метриками процесу збору:

**Панелі дашборду:**
- 📈 **Воронка збору** — конверсія (реєстрація → завершення)
- 🔴 **Проблемні документи** — топ документів з найвищим % відхилень AI
- ⏱️ **Середній час збору** — від реєстрації до повного комплекту
- 🤖 **Ефективність AI** — % автоматичних прийняттів vs ручна перевірка

---

## 📈 Бізнес-результати

| Метрика | До впровадження | Після впровадження |
|---------|-----------------|-------------------|
| Час збору повного пакету | 2-3 тижні | 3-5 днів |
| Час менеджера на клієнта | ~4 години | ~30 хвилин |
| % некоректних документів | ~35% | ~8% (AI фільтрує) |
| Клієнтів одночасно | 15-20 | 200+ |
| Втрата клієнтів через складність | ~25% | ~5% |

### ROI проєкту:
- **Економія часу менеджерів**: 80+ годин/місяць
- **Збільшення пропускної здатності**: x10
- **Зниження операційних витрат**: ~40%

---

## 🛠️ Технологічний стек

| Компонент | Технологія | Призначення |
|-----------|------------|-------------|
| **Bot Framework** | python-telegram-bot 20.x | Async Telegram Bot API |
| **AI Vision** | OpenAI GPT-4o Vision | Валідація зображень |
| **AI PDF** | OpenAI Responses API | Аналіз PDF документів |
| **Database** | PostgreSQL | Клієнти, документи, валідації |
| **File Storage** | Google Drive API v3 | Організоване сховище файлів |
| **Auth** | OAuth 2.0 / Service Account | Авторизація Google |
| **Visualization** | Grafana | BI-дашборди |
| **Hosting** | Render.com | Cloud deployment |

---

## 🗄️ Структура бази даних

```sql
-- Клієнти
CREATE TABLE docbot.clients (
    id SERIAL PRIMARY KEY,
    telegram_id BIGINT UNIQUE,
    full_name VARCHAR(255) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    status VARCHAR(50) DEFAULT 'in_progress',  -- in_progress, completed
    drive_folder_id VARCHAR(100),
    drive_folder_url TEXT,
    last_activity TIMESTAMP DEFAULT NOW(),
    created_at TIMESTAMP DEFAULT NOW()
);

-- Завантажені документи
CREATE TABLE docbot.documents (
    id SERIAL PRIMARY KEY,
    client_id INTEGER REFERENCES docbot.clients(id),
    document_type VARCHAR(50) NOT NULL,
    file_name VARCHAR(255),
    drive_file_id VARCHAR(100),
    drive_file_url TEXT,
    validation_status VARCHAR(20),  -- accepted, rejected, uncertain
    uploaded_at TIMESTAMP DEFAULT NOW()
);

-- Результати AI-валідації
CREATE TABLE docbot.document_validations (
    id SERIAL PRIMARY KEY,
    document_id INTEGER REFERENCES docbot.documents(id),
    validation_status VARCHAR(20) NOT NULL,
    ai_response JSONB,  -- Повна відповідь GPT-4
    validated_at TIMESTAMP DEFAULT NOW()
);

-- Декларації (анкети)
CREATE TABLE docbot.declarations (
    id SERIAL PRIMARY KEY,
    client_id INTEGER REFERENCES docbot.clients(id),
    status VARCHAR(20) DEFAULT 'in_progress',
    -- 17 полів для відповідей на питання анкети
    email_password TEXT,
    living_address_2022_2025 TEXT,
    property_alienation_self TEXT,
    -- ... інші поля
    completed_at TIMESTAMP
);

-- Лог нагадувань
CREATE TABLE docbot.reminders_log (
    id SERIAL PRIMARY KEY,
    client_id INTEGER REFERENCES docbot.clients(id),
    days_inactive INTEGER,
    sent_at TIMESTAMP DEFAULT NOW()
);
```

---


## 🔄 Flow клієнта

```
1. /start → Відео-інструкція → Реєстрація (ПІБ + телефон)
                    ↓
2. Створення папки на Google Drive
                    ↓
3. Чек-лист документів (візуальний прогрес)
                    ↓
4. Завантаження документа → AI-перевірка
        ↓                       ↓
   ✅ ACCEPTED              ❌ REJECTED
   Зберігається            Пояснення помилки
   на Drive                + інструкція
        ↓                       ↓
5. Оновлення прогресу      Повторне завантаження
        ↓
6. 9/9 документів → 🎉 Бонус від компанії!
        ↓
7. Анкета декларації (17 питань)
        ↓
8. Статус: COMPLETED → Передача юристам
```

---

## 📁 Структура проєкту

```
documents_bot/
├── telegram_bot.py          # Основний модуль бота (handlers, DB, Drive)
├── ai_document_validator.py # AI-валідація (GPT-4 Vision + PDF)
├── prompts.py               # Промпти для кожного типу документа
├── generate_oauth_token.py  # Утиліта генерації OAuth токена
├── requirements.txt         # Залежності
└── runtime.txt             # Python version для Render
```

---

## 👤 Автор

**Andrii** — Data Analyst / Automation Engineer

[![LinkedIn](https://img.shields.io/badge/LinkedIn-Connect-0A66C2?logo=linkedin)](# 📄 AI-Powered Document Collection System

**Інтелектуальна система збору та валідації документів клієнтів з AI-перевіркою та аналітикою**

![Python](https://img.shields.io/badge/Python-3.9+-blue?logo=python&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-13+-336791?logo=postgresql&logoColor=white)
![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4o%20Vision-412991?logo=openai&logoColor=white)
![Telegram](https://img.shields.io/badge/Telegram-Bot%20API-26A5E4?logo=telegram&logoColor=white)
![Google Drive](https://img.shields.io/badge/Google%20Drive-API-4285F4?logo=googledrive&logoColor=white)

---

## 🎯 Про проєкт

Розробив повноцінну систему автоматизованого збору документів клієнтів для юридичної компанії, яка спеціалізується на процедурах банкрутства. Система включає AI-валідацію документів через GPT-4 Vision, автоматичну організацію файлів на Google Drive, та комплексну аналітику процесу збору.

**Бізнес-проблема:** Ручний збір документів від клієнтів займав тижні, менеджери витрачали до 60% робочого часу на комунікацію та перевірку коректності завантажених файлів.

---

## 🏗️ Архітектура системи

```
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│     CLIENT       │     │    TELEGRAM      │     │   AI VALIDATION  │
│                  │────▶│      BOT         │────▶│                  │
│  Telegram App    │     │  Document Flow   │     │  GPT-4o Vision   │
│                  │     │  Progress Track  │     │  PDF Analysis    │
└──────────────────┘     └────────┬─────────┘     └────────┬─────────┘
                                  │                        │
                                  ▼                        ▼
                         ┌──────────────────┐     ┌──────────────────┐
                         │   POSTGRESQL     │     │  GOOGLE DRIVE    │
                         │                  │     │                  │
                         │  Client Data     │     │  Organized       │
                         │  Documents Log   │     │  File Storage    │
                         │  AI Validations  │     │  Client Folders  │
                         └────────┬─────────┘     └──────────────────┘
                                  │
                                  ▼
                         ┌──────────────────┐
                         │    GRAFANA       │
                         │                  │
                         │  KPI Dashboard   │
                         │  Problem Docs    │
                         │  Team Analytics  │
                         └──────────────────┘
```

---

## ✨ Ключові можливості

### 🤖 AI-валідація документів (GPT-4o Vision)

Інтегрував **OpenAI GPT-4o Vision API** для автоматичної перевірки документів:

- **Двоетапна перевірка**: якість зображення → тип документа
- **9 типів документів** з унікальними промптами валідації
- **Типізовані коди помилок** для зрозумілого фідбеку клієнтам
- **Підтримка PDF** через OpenAI Responses API
- **"Лояльна" валідація** — система приймає документи різних форматів (фото телефону, скріншоти, скани)

```python
# Приклад результату валідації
ValidationResult(
    status='rejected',
    error_code='foreign_passport',  # Закордонний замість внутрішнього
    reason='Потрібен внутрішній паспорт'
)
```

**Типи помилок, які розпізнає AI:**
| Код | Опис | Дія |
|-----|------|-----|
| `poor_quality` | Розмите зображення | Перезавантажити |
| `wrong_document` | Не той тип документа | Пояснення + інструкція |
| `foreign_passport` | Закордонний паспорт | Потрібен внутрішній |
| `drivers_license` | Водійське замість паспорта | Чітке пояснення |
| `uncertain` | Потребує ручної перевірки | Передається менеджеру |

### 📱 Telegram Bot з прогрес-трекінгом

- **Візуальний чек-лист** з прогрес-баром для кожного клієнта
- **9 обов'язкових + 4 додаткових** типи документів
- **Відео-інструкції** для кожного типу документа
- **Анкета декларації** з 17 питаннями (збереження прогресу)
- **Дедуплікація** та захист від повторних завантажень
- **Автоматичні нагадування** неактивним клієнтам (3, 6, 9+ днів)

### 📁 Автоматична організація на Google Drive

- **Структурована папка клієнта**: `Прізвище Ім'я | +380XXXXXXXXX`
- **Підпапки по категоріях**: Особисті документи, Кредитні договори, Декларація, тощо
- **Автоматичне перейменування файлів**: `Паспорт_Іванов_Петро.pdf`
- **OAuth 2.0 + Service Account** підтримка

### 📊 Аналітика в Grafana

Побудував дашборд з ключовими метриками процесу збору:

**Панелі дашборду:**
- 📈 **Воронка збору** — конверсія по етапах (реєстрація → завершення)
- 🔴 **Проблемні документи** — топ документів з найвищим % відхилень AI
- ⏱️ **Середній час збору** — від реєстрації до повного комплекту
- 👥 **Активність клієнтів** — нові/активні/неактивні за період
- 🤖 **Ефективність AI** — % автоматичних прийняттів vs ручна перевірка
- 📋 **Найскладніші документи** — аналіз bottleneck-ів процесу

---

## 📈 Бізнес-результати

| Метрика | До впровадження | Після впровадження |
|---------|-----------------|-------------------|
| Час збору повного пакету | 2-3 тижні | 3-5 днів |
| Час менеджера на клієнта | ~4 години | ~30 хвилин |
| % некоректних документів | ~35% | ~8% (AI фільтрує) |
| Клієнтів одночасно | 15-20 | 100+ |
| Втрата клієнтів через складність | ~25% | ~5% |

### ROI проєкту:
- **Економія часу менеджерів**: 80+ годин/місяць
- **Збільшення пропускної здатності**: x5
- **Зниження операційних витрат**: ~40%

---

## 🛠️ Технологічний стек

| Компонент | Технологія | Призначення |
|-----------|------------|-------------|
| **Bot Framework** | python-telegram-bot 20.x | Async Telegram Bot API |
| **AI Vision** | OpenAI GPT-4o Vision | Валідація зображень |
| **AI PDF** | OpenAI Responses API | Аналіз PDF документів |
| **Database** | PostgreSQL | Клієнти, документи, валідації |
| **File Storage** | Google Drive API v3 | Організоване сховище файлів |
| **Auth** | OAuth 2.0 / Service Account | Авторизація Google |
| **Visualization** | Grafana | BI-дашборди |
| **Hosting** | Render.com | Cloud deployment |

---

## 🗄️ Структура бази даних

```sql
-- Клієнти
CREATE TABLE docbot.clients (
    id SERIAL PRIMARY KEY,
    telegram_id BIGINT UNIQUE,
    full_name VARCHAR(255) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    status VARCHAR(50) DEFAULT 'in_progress',  -- in_progress, completed
    drive_folder_id VARCHAR(100),
    drive_folder_url TEXT,
    last_activity TIMESTAMP DEFAULT NOW(),
    created_at TIMESTAMP DEFAULT NOW()
);

-- Завантажені документи
CREATE TABLE docbot.documents (
    id SERIAL PRIMARY KEY,
    client_id INTEGER REFERENCES docbot.clients(id),
    document_type VARCHAR(50) NOT NULL,
    file_name VARCHAR(255),
    drive_file_id VARCHAR(100),
    drive_file_url TEXT,
    validation_status VARCHAR(20),  -- accepted, rejected, uncertain
    uploaded_at TIMESTAMP DEFAULT NOW()
);

-- Результати AI-валідації
CREATE TABLE docbot.document_validations (
    id SERIAL PRIMARY KEY,
    document_id INTEGER REFERENCES docbot.documents(id),
    validation_status VARCHAR(20) NOT NULL,
    ai_response JSONB,  -- Повна відповідь GPT-4
    validated_at TIMESTAMP DEFAULT NOW()
);

-- Декларації (анкети)
CREATE TABLE docbot.declarations (
    id SERIAL PRIMARY KEY,
    client_id INTEGER REFERENCES docbot.clients(id),
    status VARCHAR(20) DEFAULT 'in_progress',
    -- 17 полів для відповідей на питання анкети
    email_password TEXT,
    living_address_2022_2025 TEXT,
    property_alienation_self TEXT,
    -- ... інші поля
    completed_at TIMESTAMP
);

-- Лог нагадувань
CREATE TABLE docbot.reminders_log (
    id SERIAL PRIMARY KEY,
    client_id INTEGER REFERENCES docbot.clients(id),
    days_inactive INTEGER,
    sent_at TIMESTAMP DEFAULT NOW()
);
```

---

## 📋 Типи документів

### Обов'язкові (9):
| Код | Документ | AI-валідація |
|-----|----------|--------------|
| `passport` | Паспорт + ІПН | ✅ GPT-4 Vision |
| `ecp` | Електронний цифровий підпис | ✅ |
| `ecpass` | Пароль від ЕЦП | ❌ (текст) |
| `registration` | Довідка про склад сім'ї | ✅ |
| `credit_contracts` | Кредитні договори | ✅ |
| `bank_statements` | Виписки з банків | ✅ |
| `expenses` | Підтвердження витрат | ✅ |
| `story` | Історія клієнта (Word) | ❌ |
| `debt_certificates` | Довідки про заборгованість | ✅ |

### Додаткові (4):
| Код | Документ |
|-----|----------|
| `workbook` | Копія трудової книжки |
| `family_income` | Доходи членів сім'ї |
| `executive` | Виконавчі провадження |
| `additional_docs` | Інші документи |

---

## 🔄 Flow клієнта

```
1. /start → Відео-інструкція → Реєстрація (ПІБ + телефон)
                    ↓
2. Створення папки на Google Drive
                    ↓
3. Чек-лист документів (візуальний прогрес)
                    ↓
4. Завантаження документа → AI-перевірка
        ↓                       ↓
   ✅ ACCEPTED              ❌ REJECTED
   Зберігається            Пояснення помилки
   на Drive                + інструкція
        ↓                       ↓
5. Оновлення прогресу      Повторне завантаження
        ↓
6. 9/9 документів → 🎉 Бонус від компанії!
        ↓
7. Анкета декларації (17 питань)
        ↓
8. Статус: COMPLETED → Передача юристам
```

---

## 📁 Структура проєкту

```
documents_bot/
├── telegram_bot.py          # Основний модуль бота (handlers, DB, Drive)
├── ai_document_validator.py # AI-валідація (GPT-4 Vision + PDF)
├── prompts.py               # Промпти для кожного типу документа
├── generate_oauth_token.py  # Утиліта генерації OAuth токена
├── requirements.txt         # Залежності
└── runtime.txt             # Python version для Render
```

---

## 👤 Автор

**Andrii** — Data Analyst / Automation Engineer

[![LinkedIn](https://img.shields.io/badge/LinkedIn-Connect-0A66C2?logo=linkedin)](https://www.linkedin.com/in/prodby4ndry/)
[![Telegram](https://img.shields.io/badge/Telegram-Contact-26A5E4?logo=telegram)](https://t.me/prodby4ndry)

---

---

*Проєкт демонструє інтеграцію AI (GPT-4 Vision) у реальний бізнес-процес з вимірюваним ROI та повним циклом від збору даних до аналітики*
